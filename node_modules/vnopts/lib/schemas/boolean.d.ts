import { Schema, SchemaParameters } from '../schema';
import { ValidateResult } from '../types';
export declare class BooleanSchema extends Schema<boolean, SchemaParameters<boolean>> {
    expected(): string;
    validate(value: unknown): ValidateResult;
}
