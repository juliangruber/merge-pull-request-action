import { Schema, SchemaParameters } from '../schema';
import { Utils, ValidateResult } from '../types';
export declare class NumberSchema extends Schema<number, SchemaParameters<number>> {
    expected(): string;
    validate(value: unknown, _utils: Utils): ValidateResult;
}
