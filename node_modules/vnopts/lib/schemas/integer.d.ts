import { Utils, ValidateResult } from '../types';
import { NumberSchema } from './number';
export declare class IntegerSchema extends NumberSchema {
    expected(): string;
    validate(value: unknown, utils: Utils): ValidateResult;
}
