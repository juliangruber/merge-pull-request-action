import { Schema, SchemaParameters } from '../schema';
import { ValidateResult } from '../types';
export declare class AnySchema extends Schema<any, SchemaParameters<any>> {
    expected(): string;
    validate(): ValidateResult;
}
