import { Schema, SchemaParameters } from '../schema';
import { RedirectResult, Utils, ValidateResult } from '../types';
interface AliasSchemaParameters extends SchemaParameters<any> {
    sourceName: string;
}
export declare class AliasSchema extends Schema<any, AliasSchemaParameters> {
    private _sourceName;
    constructor(parameters: AliasSchemaParameters);
    expected(utils: Utils): string;
    validate(value: unknown, utils: Utils): ValidateResult;
    redirect(_value: this['_valueType'], _utils: Utils): RedirectResult<this['_valueType']>;
}
export {};
