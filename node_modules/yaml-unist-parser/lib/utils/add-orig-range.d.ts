import * as YAML from "yaml";
declare module "yaml" {
    namespace cst {
        interface Range {
            origStart: number;
            origEnd: number;
        }
        interface FlowChar {
            origOffset: number;
        }
    }
}
export declare function addOrigRange(cst: YAML.ParsedCST): void;
