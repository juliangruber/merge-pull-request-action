import * as YAML from "yaml";
export declare function getFlowMapItemAdditionalRanges(cstNodes: YAML.cst.FlowMap["items"]): {
    additionalKeyRange: {
        origStart: number;
        origEnd: number;
    } | null;
    additionalValueRange: {
        origStart: number;
        origEnd: number;
    } | null;
};
