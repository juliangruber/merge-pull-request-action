"use strict";
exports.__esModule = true;
function createSlicer(array, start) {
    var index = start;
    return function (end) { return array.slice(index, (index = end)); };
}
exports.createSlicer = createSlicer;
