import { Comment, Directive, DocumentHead, Position } from "../types";
export declare function createDocumentHead(position: Position, children: Directive[], endComments: Comment[], trailingComment: null | Comment): DocumentHead;
