import { Comment, Document, DocumentBody, DocumentHead, Position } from "../types";
export declare function createDocument(position: Position, head: DocumentHead, body: DocumentBody, trailingComment: null | Comment): Document;
