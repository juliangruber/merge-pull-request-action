import { Context } from "../transform";
import { Position } from "../types";
export interface Range {
    origStart: number;
    origEnd: number;
}
export declare function transformRange(range: Range, context: Context): Position;
